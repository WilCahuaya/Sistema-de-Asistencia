# App móvil (React Native)

Aquí irá el proyecto **React Native** (p. ej. Expo) cuando lo inicialices. Por ahora solo marca el lugar en el monorepo.

```bash
# Ejemplo futuro (Expo)
npx create-expo-app@latest . --template blank-typescript
```

Dependencias compartidas: `@asistencia/shared` desde `packages/shared`.
